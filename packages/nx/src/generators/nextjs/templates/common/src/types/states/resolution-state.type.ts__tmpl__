import type { ResolutionBreakpoints } from '@<%= projectName %>/constants/config/resolution-breakpoints.enum';

/** Resolution state type */
export type ResolutionState = {
	/** Current breakpoint */
	resolution: ResolutionBreakpoints;
	/** True if is a mobile resolution */
	isMobile: boolean;
};
