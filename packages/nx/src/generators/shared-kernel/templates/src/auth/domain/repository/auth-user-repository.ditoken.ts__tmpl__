import { Inject } from '@nestjs/common';

/** Auth's repository dependency injection token */
export const DITokenAuthRepository = Symbol('IAuthRepository');

/** Auth's repository dependency injection decorator */
export const InjectAuthRepository = () => Inject(DITokenAuthRepository);
