{
	"$schema": "http://json-schema.org/schema",
	"cli": "nx",
	"id": "NextShared",
	"title": "Next shared generator",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"description": "",
			"default": "next-shared",
			"$default": {
				"$source": "argv",
				"index": 0
			},
			"x-prompt": "Project name"
		},
		"tags": {
			"type": "string",
			"description": "Add tags to the project (used for linting)",
			"alias": "t"
		}
	},
	"required": ["name"]
}
