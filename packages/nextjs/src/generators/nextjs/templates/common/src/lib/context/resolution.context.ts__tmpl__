import { ResolutionState } from '@<%= projectName %>/types/states/resolution-state.type';
import { createContext } from 'react';

export const ResolutionContext = createContext<ResolutionState>({} as any);
