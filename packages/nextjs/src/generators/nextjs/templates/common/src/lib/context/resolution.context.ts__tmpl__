import { ResolutionState } from '@<%= projectName %>/src/types/states/resolution-state.type';
import { createContext } from 'react';

export const ResolutionContext = createContext<ResolutionState>({} as any);
